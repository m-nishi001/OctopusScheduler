{
  "name": "@octopus/server",
  "version": "1.0.0",
  "description": "GAS サーバ側のエンドポイント。単一の Google Apps Script プロジェクトとしてデプロイする。",
  "private": true,
  "type": "module",
  "main": "dist/server.js",
  "scripts": {
    "build": "rimraf dist && tsc -b && node esbuild.config.js",
    "lint": "eslint src",
    "typecheck": "tsc -b"
  },
  "dependencies": {
    "@octopus/core": "*"
  },
  "devDependencies": {
    "@types/google-apps-script": "^1.0.83",
    "@types/node": "^20.12.12"
  }
}
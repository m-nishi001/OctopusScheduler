{
  "compilerOptions": {
    "target": "ES2020", // Target environment (GAS V8 runtime supports modern JS)
    "module": "ESNext", // Use modern module system, ESBuild will handle bundling
    "lib": ["ESNext"],
    "useDefineForClassFields": true,
    "strict": true, // Enable all strict type-checking options
    "esModuleInterop": true, // Enables emit interoperability between CommonJS and ES Modules
    "skipLibCheck": true, // Skip type checking of declaration files
    "forceConsistentCasingInFileNames": true, // Disallow inconsistently-cased references to the same file.
    "moduleResolution": "node", // How modules get resolved (node is common for build tools)
    "resolveJsonModule": true, // Allow importing JSON files
    "noEmit": true, // Do not emit output files (JS). Rely on ESBuild for bundling. Set true if using `tsc` only for type checking without `--noEmit` flag.
    "declaration": true,
    "declarationDir": "dist/types",
    "rootDir": "src", // Specifies the root directory of input files
    "experimentalDecorators": true,
    "emitDecoratorMetadata": true
  },
  "include": ["src/**/*"],
  "exclude": [
    "node_modules", // Exclude dependencies
    "dist" // Exclude build output directory
  ]
}
